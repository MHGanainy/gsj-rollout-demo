# FINDINGS — the demo repo's register

The external F-series continues here. F-01–F-53 — the trainer-side
consumer record — live in
[gsj-harness-rollout-server-examples/FINDINGS.md](https://github.com/MHGanainy/gsj-harness-rollout-server-examples/blob/main/FINDINGS.md);
this file carries the demo's rows, F-54 onward — F-54–F-68 minted at
library CP-36 (the first from-nothing stranger test — the library's
`docs/reports/CP-36.md` is where each row's full story lives; F-54's
durable half: the mcp image closed at library CP-61, the sandbox image
at library CP-64 — the arc is closed), F-69 at
library CP-40 (the audit's orphan pass), F-79 in the examples repo at
library CP-69, **F-80 here at library CP-81** (below), and **F-83–F-86 here at library CP-92** — what four containerised strangers found in the demo, **F-87–F-92 here at library CP-94** — what round three's four found against 0.1.10, and **F-93–F-100 here at library CP-96** — what round four's four found against 0.1.11, and **F-101–F-105 here at library CP-99** — library row 100's demo half plus what round five's two demo-door strangers found against 0.1.12, and **F-106–F-117 here at library CP-101** — what round six's two demo-door strangers found against 0.1.13, plus the demo half of library row 107 (below), and **F-118–F-120 here at library CP-103** — the instrument before the last round: row 3's prompt measured, re-shaped and measured again, what step 1b tells a reader whose t=4 episode went to precedent, and the `page:N` grounding check priced (F-115–F-117's events fired in the same lift), and **F-121–F-128 here at library CP-104** — what the last round's two demo-door strangers found against 0.1.14. **The F-series is ONE register across both consumer
repos: a new row, minted from either file, takes the next id after the
highest anywhere. Next fresh id: F-129.** F-81 is the examples' deferred
training row; F-82 below records the reader fixes and remaining capture proof.
Severity vocabulary here is
CP-36's: blocking / costly / cosmetic / wrong shape, pass-tagged.
`library-side?` marks the half a demo commit cannot cure.

**[library CP-94 — 2026-09-07] Round three: four containerised strangers
against 0.1.10, both doors, four accepted episodes — and what they found
anyway.** The demo door, which never once passed `up` in round two, passed
twice (a codex and a claude agent, each on a borrowed `qwen3.6-27b`
endpoint); every row below is what the two demo strangers found *after*
reaching `1 accepted, 0 quarantined`. Their own errors are recorded in the
library's report and not converted into rows: an `EXIT=0` that was `tail`'s
in a pipeline, a `python` that was not on PATH, a README display truncated by
the reader's own tool. Fixed here: `bootstrap.py` (the lock behind `status`'s
ACTIVE state, the pull heartbeat, `provenance.engine` re-recorded and the
`coverage` block in `pins.gsj.json`, the printout's row note and `sessions/`
line), the README (two new sections, the walkthrough's row and decisions
step, the roster, the comma, the pull, the export's discriminator and
census, the clone order) and `config.yaml.example` (which file the
end-of-turn row reads after `up`). Recorded, not fixed — outside this
checkpoint's lift: `read.py` and `preflight.py` (F-87's reader line,
F-92's family test, F-90's row text).

**[library CP-83 — audit phase 1]** The walkthrough now submits row 2,
the generated bank's `case_orchard@2` / `free:boundary-evidence`, matching
the displayed transcript. Public evidence links point to the tracked
charter and checks specification; the private CP-48 record stays private.
F-73's stale disposition is reconciled below. The reader's result joins,
write-success claims and malformed-input handling remain phase 4's; no
reader behavior or citation reward changed here.

| # | severity | finding | library-side? | status |
| --- | --- | --- | --- | --- |
| F-92 | cosmetic → misleading [pass: claude-A finding 9, 2026-09-07 (UTC) — library CP-94] | `./read.py export` on a `qwen3.6-27b` episode says `think_tokens_basis: "not computed: non-Qwen tokenizer"` — the served id the README tells you to copy byte-for-byte is the lowercase alias `qwen3.6-27b`, and `read.py` decides the family by a name-shaped test (`model_used.startswith("Qwen")`) though `/v1/models` `root` says `Qwen/Qwen3.6-27B`. A thinking-off run, so nothing was miscounted; a thinking-on one would report no think tokens for a Qwen model | no (`read.py`, not lifted at CP-94) | RECORDED. `waiting-on: a gsj-rollout-demo commit that bumps read.py's export format (gsj-demo-episode-export/3) — the family test moves to a tokenizer probe or a case-insensitive family match then` |

**[library CP-96 — 2026-09-08] Round four: four containerised strangers
against 0.1.11 — the release that carried round three's fixes — both doors,
six accepted episodes, and `./bootstrap.py up` never once exiting 0 in
either A-door container.** Every long silence and every hard failure was in
a phase CP-94 had not instrumented: container create, `compose up`, the
decisions embed and the two readiness probes. The rows below are the demo's
share; the library's is its register rows 94–99. One correction against a
stranger: a2's "9 h 22 m decisions embed" and "timed out by 19×" were
measured across the operator's laptop sleeping — withdrawn as measurements;
the wall-clock deadline, the mixed clocks and the silent phase are source
facts and stand.

| # | severity | finding | library-side? | status |
| --- | --- | --- | --- | --- |
| F-93 | **blocking (any copy-on-create daemon)** [pass: codex-A twice, claude-A once, 2026-09-07/08 (UTC) — library CP-96] | `bootstrap.py:1149`'s Polar readiness probe was a `docker run --rm` of the 465 MiB Polar image under a hardcoded `timeout=90`, to make two HTTP calls under a 60 s poll loop. On `vfs` the create alone outlasts the 90 s (the three Polar containers created moments earlier had just demonstrated ~2 minutes each), so it can never pass there however often `up` is re-run — and it failed as a bare `subprocess.TimeoutExpired` traceback, the one path in the script with no `FAIL — what to do` block, three times with the estate healthy underneath (`polar-rollout:8080/health` → `{"status":"ok","nodes":1}`, the exact condition being tested). And a killed `docker run --rm` never fires its `--rm`: every failed probe left a `Created` container from the Polar image behind (`friendly_hopper`, still there hours later) | the same class in the library's `gateway_host` probe (row 94 there — b2 lost 23 minutes of successful work to it), fixed in both codebases at CP-96 | FIXED at this commit: `in_net_python` execs into one of this estate's own running Polar containers — nothing is created, nothing can leak; only with none running does it fall back to a `docker run` NAMED `gsj-demo-probe-<pid>` that a `finally` removes; a timeout comes back as exit 124 with a sentence, `polar_up` says the estate stands and goes on, `check_engine` reads the sentinel |
| F-94 | costly [pass: claude-A finding 4] | `./bootstrap.py status` crashed `KeyError: 'mcp'` at `:1206` in exactly the state a failed `up` leaves behind (`run.json` holds `forgejo` and `compose.mcp`, no `mcp`, no `ports`) — "`status` is excellent when the estate is whole and dies when it is broken, which is the reverse of when an operator needs it"; the three lines it printed before dying were already the right answer | no (the library's own `status` got its three states at CP-94; this is the demo's other code path) | FIXED: `partial_record_rows` — one row per service in `up`'s order, reached / not reached, the retrieval container's `/health` asked when the compose block names it, the resume cure; the on-disk recipe named when it exists |
| F-95 | costly [pass: claude-A finding 5] | the library's REFUSED block prescribed `--ingest-timeout`; `bootstrap.py up --help` took four flags and its own FAIL text said, two paragraphs later, that it forwards only `--overwrite-repos`, `--rebuild`, `--retarget`, `--forgejo-image` — the prescribed cure was unreachable through the documented interface | no (the flag is the library's; forwarding is the demo's) | FIXED: `--ingest-timeout SECONDS` forwarded; the FAIL text names five flags |
| F-96 | costly [pass: claude-A finding 7; codex-A, both attempts] | when `up` fails late the walkthrough's `docker run` one-liner never prints ("You have just run `up` and its final printout ended with…" — two strangers never got one), and the documented fallback, `status`, was F-94's crash | no | FIXED: `work/estate/submit.sh` is written the moment the record holds the ports — before the Polar leg — so it survives a later phase failing; `status` names it on a partial record and the printout points at it |
| F-97 | costly [pass: claude-A findings 6 + 14] | preflight's pre-`up` `end-of-turn id` FAIL offered two cures joined by "or", the second — `set end_of_turn_token_id: 248046 in config.yaml` — the very thing the README forbids in bold, because an explicit value overrides the derivation the run exists to test ("the tool wins by proximity"); and preflight exits 1 on that expected pre-`up` FAIL on a healthy estate the README invites you to preflight early | no | FIXED: with no `work/estate/pins.gsj.json` and no explicit key the tool knows the derivation has not run — a `[warn]` that says so and says NOT to set the key; the docstring and README state the exit codes (0 no FAIL row, 1 otherwise; a healthy endpoint exits 0 before and after `up`); an explicit disagreeing value still FAILs |
| F-98 | costly [pass: claude-B (b2) proposal row 3; claude-A finding 9] | the sandbox pre-check shells out to Docker and refuses early, but read no storage driver: `vfs` was prescribed as a cure with no price, and the price surfaced post-mortem — ~13 GB per sandbox container, minutes per create, Polar's 600 s sandbox-create budget blown twice (`docker create failed with exit code -1: None`) | half: the library's `check_daemon` runs the same `docker info` — the one-field read is its at the next `estate.py` lift (row 94 records it) | FIXED: `check_docker` reads `{{.Driver}}` and says what a copy-on-create driver costs, at the first Docker call, before anything is pulled or created |
| F-99 | cosmetic → misleading [pass: claude-A (minor, a near miss); codex-A finding 4 — library CP-94's own work, half-right] | the pull heartbeat measured received bytes only: "26.0 KiB in the last 60s — the pipe is moving" printed through forty minutes of extraction, when no bytes are expected, and a stranger counted `Download complete` against `Pull complete` by hand to tell a phase change from a stall | the library's `image_pull` had the same heartbeat — fixed there at CP-96 (row 99) | FIXED: `ensure_image` streams `docker pull` through a `popen` seam, tallies docker's own per-layer state lines, names the phase in every heartbeat (`N/M layers complete, K extracting …`) and says when no bytes are expected |
| F-100 | cosmetic → costly [pass: claude-A findings 8, 11, 12 and the README measured against reality] | five README claims: "pull the four images" (`bootstrap.py` pulls three; the bring-up pulls Forgejo itself, said 200 lines away — a stranger believed the pull phase over 92 minutes in and then met a fourth); `pip install` with no slow-pipe guidance while `docker pull` has three bullets (it failed outright for two strangers on pip's 15 s default, as the first command either ran); `docker system df` as the second liveness check (flat through an actively progressing pull on Docker 29.8, which accounts an image only once complete — `df -h /` on the data root moved 1–2 GB every two minutes all night); the `~6 GB` disk figure and the unpriced `vfs` recommendation (~60 GB of image growth measured); and "embeds 317 pieces in about 5 seconds" with no hardware label — `up`'s own "above ~5,000 units … minutes, not seconds" hedge pointed the wrong way for a 313-unit drop | no | FIXED in the README: each sentence corrected against the measurement — three images plus Forgejo, `pip install --timeout 120 --retries 5`, `df -h` on the data root instead of `docker system df`, the driver's price, the embed figure labelled Apple Silicon idle with the contended-host truth beside it and `--ingest-timeout` named |

**[library CP-103 — 2026-09-09] The instrument before the last round: one
demo sitting, no round, no release.** Library CP-102 ran the walkthrough's
step 1b as its author on a fresh clone and found the pair demonstrating the
checkout half only — at t=4 the 0.6B floor model never called
`mcp_gsj_search_case`; row 3's prompt took it to the decisions tool, a
docket for a "registry number" and a `page:7` no checkout holds. Round seven
is the last round, and an instrument whose evidence value depends on which
tool a floor model reaches for closes a row on luck or not at all. This
sitting measured the shipped prompt and three re-wordings on the same estate
(5 samples each at t=4, the winner and the control at t=2 as well), changed
the generator, said in step 1b what a reader checks when the agent goes
elsewhere, and re-ran the pair from a fresh clone on the published
`gsj-polar:f0e8343a-gsj0.1.14`. The rows are F-118–F-120; F-115–F-117's
events fired in this lift and their rows say what was measured.

| # | severity | finding | library-side? | status |
| --- | --- | --- | --- | --- |
| F-118 | **costly → wrong shape** (an instrument that answers by luck) [pass: library CP-102's own fresh-clone run of step 1b, 2026-09-09; measured here at CP-103] | row 3's prompt — "Does any recorded easement or right of way affect the disputed strip? Name the registry number and cite the page" — is a question a precedent search can plausibly answer, and the 0.6B floor model answered it from `mcp_gsj_search_decisions` alone: CP-102's one run, and **4 of 5** samples here (a docket, `GREV000082013`, as the "registry number"; `page:7` in three answers, a page no checkout holds; **1 of 5** opened the case file). Both episodes of the pair are *accepted* either way — acceptance checks provenance — so the walkthrough's "the t=4 episode can find, read and cite it" was a *can* the instrument could not deliver on demand | half: the library's row 107 waits on a stranger running the pair; the prompt is this corpus's | **FIXED in `synthetic/make_corpus.py`** — row 3 now reads *"Is there an easement deed in the case file so far? If so, which page records it, what is the deed number and when was it registered? Cite the page as (page:N)."* — the case file named as the source (row 2's own anchor, "so far"), the facts asked for (deed no. 98-4417, 11 August 1998) on page 4 alone and in no rendered decision, the page in the form the agent is taught, no tool named. Measured on the floor model: **5 of 5** samples at t=4 called `mcp_gsj_search_case`, and every one named 98-4417, the date and page 4 (on an exploratory estate stood from this tree; the fresh-clone run of the pair after this commit, on the published `gsj-polar:f0e8343a-gsj0.1.14`, is recorded on the library's register row 107 and in its CP-103 report). Two re-wordings were measured and rejected because an "if the file does not contain one, say so" clause let the model write that sentence with **no tool call** — 3 of 5 and 5 of 5 samples at t=4, where the deed exists. `tests/test_corpus_cp103.py` pins the row order, the prompt's shape and the deed number's single home |
| F-119 | cosmetic → costly (the README promised what the pair *shows*; a reader whose t=4 episode went to precedent had no sentence telling them what they were looking at) [pass: CP-102's run is the case a reader meets — the floor model is what the demo ships against] | step 1b said what the two transcripts demonstrate and nothing about the accepted, `findings: []` episode whose answer rests on nothing the session retrieved: `show` marks no `page:N` as ungrounded, `export`'s `page_census.pages_beyond_timestep` scans only the *hits* (so a written `page:7` at t=4 with no case-file search is invisible to it), and the `citations` line covers `dec:` tokens only | no | **FIXED in the README's step 1b**: the t=4 transcript's expected shape stated line by line (the `-> mcp_gsj_search_case` line, the page-4 hit with `all <= timestep 4`, the answer naming 98-4417); a paragraph for **when the agent goes elsewhere** — a `-> mcp_gsj_search_decisions` line and no case-file line means `search_pages_returned: null` in the export, the case file was never searched, and any page or registry number in the answer is the model's, not the corpus's; re-submit under another `--task-id`; and the same question at t=2 through the direct triple, fenced, with what the honest answer is and how the floor model met it (all five t=2 samples searched the case file, got pages 1 and 2 back and could not name the deed number — and every one then wrote that the deed is "recorded on page 1", two inventing a 2019 registration: the search result and the absent number are the evidence, the sentence is the floor model) |
| F-120 | cosmetic (a check the reader has for `dec:` and not for `page:N`) [pass: CP-103's reading of read.py, not lifted; priced, not built] | `read.py` grounds every `dec:` token the agent writes against the session's own hits (`decision_context` / `citations_in`, per token, `grounded`/`UNGROUNDED` beside the turn) and has no equivalent for a `page:N`: `page_census` reports what the searches *returned* and what lay beyond the timestep among those hits, never what the agent *wrote* — CP-102's `page:7` at t=4 passed every census clean | no (`read.py` is the demo's) | **RECORDED, priced, not built** (`read.py` not lifted at CP-103, and F-87/F-92 park the next export shape change on the format bump). The check as it would land: a `PAGE_RE` over the assistant text; a page context built from `mcp_gsj_search_case` hits **and** the agent's own `read`/`grep`/`bash` observations of `md/page_NNNN.md` (an agent may open a page without searching — AGENTS.md allows grep over `md/`; the `bash` case means parsing a command line, the one soft spot); a per-token verdict `grounded (search hit turn N)` / `grounded (read turn N)` / `UNGROUNDED — no search returned page N and no read opened it` / `BEYOND CUTOFF — page N > timestep T`; a `pages` header line beside `citations`, and a `page_census.tokens_written` block mirroring `decision_census.tokens_written`. What it would and would not catch, from this sitting's own samples: CP-102's `page:7` at t=4 with no case-file search → UNGROUNDED and BEYOND CUTOFF; the t=2 "recorded on page 1" confabulation → *grounded*, because page 1 was returned — the check verifies that a page was retrieved, never that the claim about it is true, exactly as the `dec:` check verifies an identifier and not a legal proposition. Roughly 40–60 lines of `read.py`, a fixture with a written-but-unretrieved page, tests in `test_reader.py`, and an export format bump — which is why it belongs with F-87/F-92's lift and not here. `waiting-on: the gsj-rollout-demo commit that bumps read.py's export format — a page:N grounding check beside the dec: one, built as priced here` |

**[library CP-104 — 2026-09-10] The last round: what round seven's two
demo-door strangers found against 0.1.14.** Both reached step 1b and ran it as
written — `case_orchard` at t=4 through the fence, the deed on page 4 cited as
`(page:4)` with its number and date — and a1 ran the same question at t=2
through the triple unprompted and got the honest negative; library row 107
closed on those bodies, as a claim about what the walkthrough shows and not
about whether the service filters (step 1b's new paragraph says where that
happens). Seven accepted episodes between them, no quarantine. What they filed:
the image-size bullet was a disk figure under the wire's label (F-121);
`bootstrap.py`'s failed-derivation path, four prints and one wrong answer, met
in one run by a2 and caught only by the post-`up` preflight (F-122, F-123 — the
README half taken, `bootstrap.py` not lifted); a1's question about the
enforcement path (F-124); seven sentences measured against the tool (F-125);
four of a2's open questions answered where it asked (F-126); and two recorded
for later sittings (F-127, F-128). The rig's faults — a2's first session killed
by the headless CLI's background ceiling mid-pull, its finished session then
held open by its own monitors — are the round's record's, not rows here; a2
took the kill as a free test of the stale-lock sentence (F-117's), which held.

| # | severity | finding | library-side? | status |
| --- | --- | --- | --- | --- |
| F-121 | cosmetic → misleading [pass: gpt6-A (a1) F1, ranked 2 of 3; claude-A (a2) FINDINGS 5 and 6 — 2026-09-09, library CP-104] | the Quickstart and the "Disk, and the wire" bullet called ~4.05 GB "compressed on the wire" and the retrieval service's 2.65 GB "44% of the total in one image". Measured at CP-104 from the four images' registry manifests: **1.79 GB (`arm64`) / 1.84 GB (`amd64`) on the wire**, the retrieval service 1.34 / 1.38 GB of it (~75%); 4.05 GB is a classic `overlay2` daemon's extracted accounting (`docker system df` 4.061 GB for a2 and for the library-door b1; the retrieval service 2.65 GB of it, 65%), and 44% is 2.65 of Docker Desktop's 6.1 — a share of neither total. a2 read the manifests itself; a1 divided | no | **FIXED in the README**: both sites carry the wire figure from the manifests and the disk figure per image store (classic `overlay2` ~4.05 GB; Docker Desktop's containerd store ~6 GB — `docker info` names its driver `overlayfs`; 6.8 GB listed on this workstation), and `docker system df` is named as the accounting check once the pulls finish (a2's FINDING 7 asked; it stays ruled out as a progress meter) |
| F-122 | costly [pass: claude-A (a2) FINDINGS 1, 2 and 3, ranked 1–3 of 8] | on a non-reference model whose tokenizer-bound derivation fails, `bootstrap.py` writes a measurement's word over an inference at three sites, and a2 met all three in one `up` on a shared endpoint that was up throughout: `derive_endpoint_pins` answers a `TimeoutError` on its first `/tokenize` with "this endpoint cannot render its own chat template over the API (not vLLM?)" and "Cure: bring the endpoint up" — the endpoint was vLLM and answered the same request in 0.25 s before and after (one request per probe, `_endpoint_json`'s 15 s timeout, no retry); `pins.gsj.json`'s `coverage.g6_expected_tail_ids` reads "derived here from the endpoint's own template render" because the coverage block keys on the model name, not on the derivation's success, while its `derived_at` says FAILED and `provenance` names the reference snapshot; and `up` exits 0 after printing "the first episode will likely be quarantined at G6". The post-`up` preflight exited 1 on it — the only gate that did | no (`bootstrap.py` is the demo's) | **README half FIXED**: step 0 says to run the preflight again after `up` and before the first submit, names the 15 s single attempt, and lists what the failure path prints that is not to be believed; "When something is rejected" gains the cause (up, one request timed out). **`bootstrap.py` half RECORDED**, not lifted at CP-104 — the fix as it would land: the `why` reports what was measured (`POST /tokenize timed out after 15 s, 1 attempt`) and drops the vLLM inference; one retry; the coverage row keyed on the derivation (`CARRIED from the reference estate — derivation from THIS endpoint FAILED: <why>`); `up` exiting non-zero when it predicts a G6 quarantine, or a `--strict`. `tests/test_readme_cp104.py` pins the README's quotes to the source, so the fix moves both. `waiting-on: the gsj-rollout-demo commit that lifts bootstrap.py` |
| F-123 | costly [pass: claude-A (a2) FINDING 4] | after a failed derivation `bootstrap.py` answers the library tool's `end_of_turn_token_id` with the reference model's 151645 (`BuilderConfig()`'s default — its comment: "also what a non-reference model gets when the endpoint derivation failed"), which the tool takes as an operator's explicit flag: the tool then measured 248046 from the same endpoint, warned `--end-of-turn-token-id 151645 disagrees with the endpoint's own render (248046); the explicit answer stands`, and named `--end-of-turn-token-id` and `--name` as the cure — flags `./bootstrap.py` does not expose. Another model's constant was strictly worse than passing nothing: the library's `choose_end_of_turn` would have taken its own measurement | no | **README half FIXED** (step 0 names the warning and says the re-run is the cure — a2's re-run took 23 s and derived 248046). **`bootstrap.py` half RECORDED**: on a failed derivation pass no `end_of_turn_token_id` answer, so the tool's measured > record > default precedence decides. `waiting-on: the gsj-rollout-demo commit that lifts bootstrap.py` |
| F-124 | cosmetic → costly [pass: gpt6-A (a1) — its mid-run question (transcript l.2544) and the first item of "What I still do not understand"; claude-A (a2) not-understood 1 and 5; the library register's row 107] | step 1b said the t=2/t=4 pair separates the checkout bound from the retrieval bound and nothing in the README said how: a1 saw the service's census carry all four orchard pages after ingest while its t=2 searches returned `[1, 2]` and t=4 `[1, 2, 3, 4]`, noted that the pair also changes the checkout branch and the model's queries, and "cannot independently isolate which part of the enforcement is checkout, authenticated service scope, filtering, or their combination"; a2 asked separately whether `read.py`'s "the cutoff holds" is a check or a re-render, and why the git host is a server | half (the mechanism is the library's; the sentence is this README's) | **FIXED**: step 1b's new paragraph, "Where each half is enforced" — the clone at `timestep-T`, shallow, remote and reflog removed, sign-in required; the service's whole-document index and its per-query `page <= T` pre-filter under a signed token's `T`; why a case-file search never reads the checkout; `read.py`'s census and the receiver's G5 as two implementations over one trace. Checked against the service's code (`index.py`'s `where` pre-filter, `tools.py`'s `case_status`, `pi_harness.py`'s token and clone) **and its live answers** at library CP-104 on this README's own estate: one query, `easement deed`, under a t=2 and a t=4 token — page 4 only under t=4 — and a t=2 token re-encoded to `T=4` under its original signature refused. The row-107 parenthetical says the row closed and what it was about |
| F-125 | cosmetic [pass: claude-A (a2) FINDING 8, "What I would change" 7 and 8, not-understood 4, its Step 9; gpt6-A (a1) F2 — its own slip, recorded as such] | seven sentences measured against the tool: `submit` "prints the `length-terminated:` line" read as a truncation marker — it prints `length-terminated: N/M` after every run (a2 took its row-2 line for truncation for a moment); the heartbeat's phase clause quoted three states and `bootstrap.py` prints a fourth (`K downloaded, waiting to extract`); the poll line quoted `building decisions: batch 1/1` and both readers saw only `the service is still working (the next collection has not reported a batch yet)` — the named form prints once a collection's first add batch lands (the library's `progress_line`), and the 317-piece drop is one batch of up to 1,000 (the service's `ADD_BATCH_SIZE`), so that collection is done by the next poll; the step-1 fence reads `$PWD` and nothing said so, while `work/estate/submit.sh` carries absolute paths; "the estate writes three" `rollout.yaml`s named two (a2 found the receiver's copy with `find`); a resumed pull re-announces every layer `Pulling fs layer`, which reads as a restart; and a new shell needs the venv (a1 ran the generator without it and got `PyYAML is missing` — the message right, the reminder cheap) | no | **FIXED in the README**, each against the tool's own source or output (`pull_phase_summary`, the library's poll line at `estate.py:2143`, `submit_recipe`, the receiver copy at `bootstrap.py:1252`, a2's re-run: the retrieval image in 16 min after 24 on the killed attempt), and pinned by `tests/test_readme_cp104.py` |
| F-126 | cosmetic [pass: claude-A (a2) "What I still do not understand" 2, 3, 6, 7] | four questions a2 left open that the README could answer in a sentence each: what the "G4 walk" is; whether `sampling_policy: UNKNOWN` makes the logprobs useless or merely unattributed; what `reward: null` is doing in the export; what `preserve_thinking: true` beside `enable_thinking: false` means | no | **FIXED where each was asked**: the G4 walk named (the library's `pins/derive_pins.py`, run where the served snapshot's files are on disk); unknown-not-unusable (on both engines this project measured, the captured logprobs matched a teacher-forced replay, so what the unknown policy hides is the sampling distribution, not the scores); `reward` is the trace's own field passed through, `null` because nothing here scores; `preserve_thinking` is a template switch pi sends on every request, which the derivation renders under because G6 compares what pi's requests render |
| F-127 | cosmetic → costly (a first reader's time) [pass: gpt6-A (a1), "What I would change", third] | "The commands worked; finding the current path required reading through a large amount of historical context": "Run it" opens with a forty-line account of what each library release carried. a2 praised the opposite of a neighbouring habit — the README's inline "a stranger nearly did X" warnings stopped it five times — and those are not the changelog | no | **RECORDED for the shipping pass**, not taken at CP-104 (restructuring the page's opening is a release sitting's, not a findings sitting's): the release-history paragraphs move to a closing history section; every inline warning stays where it fires. `waiting-on: the shipping pass's gsj-rollout-demo commit` |
| F-128 | cosmetic [pass: gpt6-A (a1), "What I would change", fifth; the library register's row 111 (b1's FINDING 3)] | the demo's own pull heartbeat (`bootstrap.py`'s `ensure_image` — the Polar, retrieval and harness pulls) says `the pipe is moving` on any received byte, the binary verdict the library's `estate.py` retired at CP-104 after round seven's library-door b1 sat twenty-four heartbeats at `7/12 layers complete` (its own log; it counted seventeen) while one layer went through docker's retry backoff three times; a1's 46-minute retrieval pull ran on it and a1 asked for an attributable per-layer progress read | no (`bootstrap.py`) | **RECORDED**: the library's `pull_tally_age` / `pull_moving_caveat` port as they are. a1's exact ask — bytes received per layer — is not measurable from this seam: docker's non-TTY pull output prints state changes, never byte counts. `waiting-on: the gsj-rollout-demo commit that lifts bootstrap.py` |

**[library CP-101 — 2026-09-09] Round six: four containerised strangers
against 0.1.13 — the release CP-100 published, with `gsj-polar` re-cut to
match it — on the same two doors and the same two agents as round five,
all four on `overlay2` (the driver axis dropped, banked at CP-99), with
**one variable changed: the operator's launch note**. Both demo-door
strangers finished: three accepted episodes between them, zero
quarantines. Neither met a single defect in the *software* this repo
drives — every row below is the README's prose against what a reader
actually met, which is what this door is for. The round's software
finding is the library's (`up`'s `== run demo ==` footer contradicting
its own pins line twenty lines above, filed here by `a1` and fixed in
the library's `estate.py` at CP-101), and its best finding is the other
door's (a whole trajectory marked ERROR by a pipeline's exit code —
library wishlist row 110). Nine rows taken here, three recorded and left
with their events named. The library's are its wishlist rows 101–110.**

| # | severity | finding | library-side? | status |
| --- | --- | --- | --- | --- |
| F-106 | cosmetic → costly (the reader most likely to be hurt is the one who skims, and this README is 1,037 lines) [pass: claude-A (a2) FINDING 1, ranked **first of nine**; 2026-09-09, library CP-101] | the prerequisites were a **comment block inside the bash fence at `:377`**, after 338 lines of measured-cost narrative, with no table of contents and no quickstart. "A reader who scrolls the first screen and starts typing will run `bootstrap.py up` without having read the `docker run --rm alpine true` precondition that the same fence makes the headline check" — the precondition two round-two runs died on. | no | **FIXED at this commit**: a `## Quickstart` section directly under the opening, carrying the two preconditions (the `alpine true` run smoke, and the Python/git/Compose line) above a seven-line fenced block of the whole path, with a pointer to the walkthrough and the pull budget stated in both figures. The measured narrative stays where it was, below it |
| F-107 | costly (three strangers in three rounds, and one documented cure that failed at 935 s) [pass: gpt6-A (a1) F2 **+** F4, ranked **first of six**; claude-A (a2) FINDING 2, "the single cheapest fix in the document"; gpt6-B (b2) F3; 2026-09-09, library CP-101] | the Install bullet has carried pip's slow-pipe cure since library CP-96 — in a **paragraph 250 lines above the fence a reader copies from**. So the copy-paste line was still the bare one: a1's plain install exited 2 after 190.21 s at 20.5/46.8 MB of `pyarrow`, its documented retry (`--timeout 120 --retries 5`) exited 2 again after **935.24 s** at 25.8 MB, and an unchanged third attempt landed — 1,242 s of install time in all. All three strangers asked for the same thing and asked for it **in the copyable block**. (The *duration* is the rig's: four concurrent installs on one NAT — recorded as the harness's, not this README's.) | no | **FIXED at this commit**: the fence's install line **is** `pip install --timeout 120 --retries 5 …`, with a comment saying why the flags are in it and that they cost a fast pipe nothing. The Install bullet's diagnosis and both signatures stay |
| F-108 | costly (a reader nearly filed a disk regression against a correct README) [pass: claude-A (a2) FINDING 4, measured; gpt6-A (a1) remarked it unprompted at l.1888; 2026-09-09, library CP-101] | stall check (2) prescribed **`df -h`** on the daemon's data root — blind at this project's bandwidth. a2 measured a flat `102G` twice on a 911 G filesystem while its own `df -k` sampler on the same filesystem moved **+214 MB/min**. This is the same false negative for which library CP-96 retired `docker system df`, in the sentence that replaced it; the library corrected its own copy at CP-99 (row 103) and **this README lagged one round behind it**. Second half, which neither document had: on a **shared data root `df` counts every tenant**, so the check is only attributable when the data root is on its own filesystem | half: fixed in the library's `troubleshooting.md` at library CP-99 — this is the demo's lagging copy | **FIXED at this commit**: `df --block-size=1M`, a2's numbers, and the shared-root caveat |
| F-109 | costly (the sentence tells you to interrupt a pull that is recovering) [pass: claude-A (a2) FINDING 6; 2026-09-09, library CP-101] | stall check (3) read "`Retrying in N seconds` / `unexpected EOF` … a hung pull eventually prints one, **a healthy one never does**" — the claim library CP-99 falsified **3/3** across five pulls of three distinct layers, every one of which completed, and corrected in `troubleshooting.md` (row 103) while this README kept it. The slash also reads as a disjunction where the signature is a **conjunction**: retries alone are the pull recovering out loud; what makes it a transport failure is the run *ending* in `unexpected EOF`. Two strangers in round five had their hands on Ctrl-C over this | half: the library's half fixed at CP-99 (row 103) — this is the demo's lagging copy, found by the round rather than swept for | **FIXED at this commit**: check (3) is written as a conjunction ("`Retrying in N seconds` **followed by** a run that ends `unexpected EOF`"), says the retry line alone is normal backoff, and records that this README used to say the opposite one sentence before telling you to interrupt |
| F-110 | cosmetic → misleading (a reader went looking for a line the README promised and could not find it) [pass: claude-A (a2) FINDING 3, left OPEN in its own transcript — it promised to settle it and the run ended first; 2026-09-09, library CP-101] | the disk bullet said, unconditionally, that "`up` names the driver at its first Docker call". It does not: `check_daemon` reads the driver from its first `docker info` and **warns only when it finds a copy-on-create driver** — on `overlay2` nothing is printed at all. Round six ran four strangers, all `overlay2`, and produced **zero** driver lines; the reader correctly could not reconcile the promise with the silence, and had no way to tell "the good case" from "the check did not run" | no (the library's `check_daemon` behaviour is correct; the demo's prose about it was not) | **FIXED at this commit**: the sentence says `up` prints this **only** on a copy-on-create driver, that on `overlay2` you correctly see no driver line and there is nothing to do, and gives `docker info --format '{{.Driver}}'` for a reader who wants to know which case they are in |
| F-111 | cosmetic (one stranger extrapolated "on the order of a day" from it before correcting itself) [pass: claude-A (a2) FINDING 5, sharpened twice with its own measurements; 2026-09-09, library CP-101] | "**~6 GB** of images" is the headline a reader uses to estimate **download time**, and it is the wrong number for that — it is disk *after extraction*. The correction ("the pull transfers less") sat inside a parenthetical about a superseded 3.5 GB estimate. a2 measured what was missing: **~4.05 GB compressed on the wire**, of which the retrieval service alone is **2.65 GB — 44% of the total in one image**, which is also why image 2 of 4 is where half an hour goes | no | **FIXED at this commit**: the bullet leads with the two figures as two figures — disk after extraction, and the compressed wire total with the retrieval image's share named — and says which one governs `df` and which governs the clock. The Quickstart repeats both |
| F-112 | cosmetic (an internal inconsistency, in the section furthest from the fix) [pass: claude-A (a2) FINDING 8; 2026-09-09, library CP-101] | the regression-suite line said `python -m pytest -q tests`, and a stock Ubuntu >= 23.04 box has **no `python` on PATH** — it works only while the venv from "Run it" is active, and this line is in the last section of a 1,037-line document that a reader dips into out of order. The README uses `python3` everywhere else it invokes an interpreter, and calls out that very class of system three sections earlier for PEP 668 | no | **FIXED at this commit**: `python3 -m pytest -q tests`, with the reason named |
| F-113 | cosmetic (two documents disagree politely and the reader arbitrates) [pass: claude-A (a2) FINDING 9 — "the fix is a *warning about* a mismatch rather than a resolution of it"; the third round to raise it; 2026-09-09, library CP-101] | library CP-94 cured the copy-the-printout trap by making the printout *say* which row the README explains. a2 grants that the fix works ("I would have copied `--row 0` without that parenthesis") and points out it is still a warning: the tool prints `--row 0`, the README says type `--row 2`, `work/estate/submit.sh` also carries `--row 0`, and the reader hand-edits while copying | no — and the printout's `--row 0` is **pinned by this repo's `tests/`**, which library CP-101's freeze-lift does not open, so the resolution had to be README-side | **FIXED at this commit**, README-side: step 1 now says **copy the fenced command below, not the printout** — the fence is already correct and complete — and explains that `--row 0` is a real episode (`case_mill@3`'s precedent prompt, the one step 3 runs) rather than a wrong one. Nothing to hand-edit; the disagreement is gone without touching the recipe |
| F-114 | (not a defect — the demo half of a library limit) [pass: library wishlist row 107, unanswered by three consecutive rounds; 2026-09-09, library CP-101] | the walkthrough runs **one row**, and one row cannot show *where* the temporal cutoff is enforced: at `t=2` the case branch holds two pages, so no later page **could** be retrieved whatever the retrieval service does — the checkout bound and the retrieval bound are indistinguishable on that evidence. Six stranger rounds have never produced the pair that separates them, because nothing asked for one (round six: a1 ran `orchard@2` and `mill@3`, b1 `@4` only, b2 `@2` only) | half: the library's row 107 stays OPEN — a round must actually run the pair; this commit only makes the README produce it as a by-product | **TAKEN at this commit**: walkthrough step **1b** points at the pair the demo's own corpus already contains — row 2 (`case_orchard@2`) beside row 3 (`case_orchard@4`), with the **1998 easement deed only on page 4** — gives the second submit verbatim, and says what the difference between the two transcripts demonstrates. A reader who follows the README now produces the evidence without being asked |
| F-115 | cosmetic (recorded, not taken) [pass: gpt6-A (a1) F1, ranked **4 of 6**, and carried into its not-understood list; 2026-09-09, library CP-101] | the Decisions section says the agent "has **eleven tools** on its roster" and, four sentences later, names "a fourth" MCP tool (`mcp_gsj_decision_stats`) — so a reader counting eight built-ins + three MCP tools against a fourth cannot tell whether the roster is eleven or twelve, or whether the fourth sits outside what G3 pins. a1 asked for "an exact roster, or an explicit statement that the fourth tool is outside it" | no | **RECORDED, not taken at this commit**: library CP-101's wall is the findings its prompt enumerates, and this is not among them. The answer is knowable from the tree (the wire array G3 hashes) and should be stated from a measurement rather than from prose. `waiting-on: the next gsj-rollout-demo commit that opens the Decisions section — the roster stated from the hashed wire array, and the fourth tool's membership said explicitly` **→ fired at library CP-103 (2026-09-09): TAKEN.** Counted from the hashed array itself (`trajectory.traces[0].tools` of an archived episode on this estate, and the packaged capture the pins are carried from): **eleven = seven pi built-ins (`read`, `ls`, `grep`, `find`, `write`, `edit`, `bash`) + four MCP tools**, `mcp_gsj_decision_stats` inside the eleven G3 hashes. The README said eight and three and then named a fourth; it now says seven and four, and says where the count comes from |
| F-116 | cosmetic (recorded, not taken) [pass: gpt6-A (a1) F7, ranked **3 of 6**, and in its not-understood list; 2026-09-09, library CP-101] | the precedent episode's header read `response 19,367 (2,756 trainable = 14%)` with `finish stop`, beside a README sentence naming an 8,192-token generation cap — and nothing says whether `response` counts one completion or a whole session, so the two look contradictory. a1 asked for the scope of each count and of the cap to be defined | no | **RECORDED, not taken at this commit** (outside library CP-101's enumerated findings). The facts are the reader's (`read.py`'s header) and the cap is per completion, not per session — but that must be **measured and stated**, not asserted here. `waiting-on: the next gsj-rollout-demo commit that opens read.py or the README's counts paragraph — the scope of response/trainable and of the generation cap, stated from the archive` **→ fired at library CP-103 (2026-09-09): TAKEN in the README's counts paragraph** (read.py not opened). Stated from the archive's own `conventions` block and measured on the CP-102 fresh-clone pair: `response` is the session's whole merged chain after the prompt — every assistant turn and every tool result replayed as context, `loss_mask` 0 on the replayed tokens (a `response 2,522` episode whose two turns span `[0, 34)` and `[2480, 2522)`: 76 trainable, the 2,446 between them one tool result); `trainable` is the `loss_mask` 1 positions; the 8,192 cap is `config.yaml`'s `max_tokens`, handed to pi as `maxTokens` **per completion** (library `pi_harness.py:130`) — so `response 19,367` with `finish stop` is a chain, not a breached cap |
| F-117 | cosmetic (recorded, not taken) [pass: claude-A (a2) FINDING 7 — it hit a stale lock, recovered correctly, and found the recovery undocumented; 2026-09-09, library CP-101] | after the harness killed a2's `up` twice, `work/.bootstrap.lock` survived with a dead pid. `status` detects a stale lock and ignores it — correct, and **nowhere written**: the README says "never delete the lock file" without saying that you never need to, so a reader whose `up` was killed is left with an instruction not to touch the thing standing in their way | no | **RECORDED, not taken at this commit** (outside library CP-101's enumerated findings, and the behaviour is `bootstrap.py`'s, verified by a2 rather than by this checkpoint). `waiting-on: the next gsj-rollout-demo commit that opens the status paragraph — one sentence saying a lock whose process is gone is detected as stale and ignored, so it never needs removing by hand` **→ fired at library CP-103 (2026-09-09): TAKEN, and measured rather than asserted.** The lock is the kernel's `flock` on `work/.bootstrap.lock` (`held_for`), and `status` asks `lock_holder()` whether anyone holds it — a file left by a killed `up` carries a dead pid and no flock, so `lock_holder()` is `None` and `status` reports the estate, not ACTIVE. `tests/test_corpus_cp103.py` writes exactly that file and measures it on the real probe; the README's status paragraph now says the lock is the flock, that a killed `up` leaves a file the next `status`/`up` ignores, and that it never needs removing by hand |

**[library CP-99 — 2026-09-09] Round five: four containerised strangers
against 0.1.12 — the release CP-97 published — on two doors (this README
and the library's PyPI page), two agents, and, for the first time, **two
storage drivers crossed against them**, so a driver finding can be told
apart from a software finding. Two strangers walked this door and both
finished: three accepted episodes between them (`a1` one, `a2` two), zero
quarantines, zero rejections. The other door lost a run to a library
defect — `up` wrote a gateway address that does not work and printed
"(measured: dialable …)" beside it — and lost another to the operator's
launch note, which two agents read as forbidding `docker info`. The
driver's price is now a controlled measurement rather than an inference:
on the identical row-2 task the `vfs` stranger's sandbox container took
**19.4 s** to create against the `overlay2` stranger's **1.1 s**, and its
data root held **31 G against 8.4 G** for the same 4.061 GB of images.
The rows below are this repo's half; the library's are its wishlist rows
101–109. F-101 is not round five's: it is library row 100's demo half,
whose token named "the demo's next commit" — this one.**

| # | severity | finding | library-side? | status |
| --- | --- | --- | --- | --- |
| F-101 | costly (a leftover container per killed probe, on the daemon where containers cost the most) [pass: library CP-97's live proof of the installed 0.1.12, corrected in the library at CP-98 — not a stranger; 2026-09-09, library CP-99] | `reap_container` (`bootstrap.py:267`) read `docker rm -f`'s **exit code**, and the real CLI exits **0** whether it removed a container or there was none (`No such container` on stderr — measured on Docker 28.5.1/API 1.51 and on a nested 29.8.0 `vfs` daemon, recorded in the library's `estate/corpus/tests/cli_shapes.json`). So the first tick always returned True, `PROBE_REAP_S`'s 30 s bound never ran, and the `gsj-demo-probe-<pid>` container a copy-on-create daemon produces *after* the client is killed was never reaped — the "still creating" sentence at `:315` was unreachable. Round five did not observe it (no demo stranger's probe was killed), which is not the same as fixed. | no | **FIXED at this commit**: *removed* is read off STDOUT — the CLI prints the name it removed — and every other answer retries to the bound; the test's fake takes the CLI's two measured shapes (`_rm_f`) and asserts the bound engaging, where it previously answered exit 1 for a missing container and proved a loop no daemon ever ran |
| F-102 | wrong shape (it contaminates the one experiment this README asks a reader to run) [pass: claude-A (a2) — noticed the leak, correctly declined to apply it, and still called the tool's agreement "a nice independent confirmation"; gpt6-A (a1) supplied it and never verified; 2026-09-08/09, library CP-99] | "0 — preflight your endpoint" published the operator's own endpoint's end-of-turn id — "*(two strangers nearly did; the derivation gave the same **248046** unaided)*" — inside the very paragraph that tells you not to write that id into `config.yaml` and to let the derivation prove itself. A reader who has read the page cannot then *derive* the value; they can only recognise it. Measured across round five: **zero of four strangers produced an uncontaminated derivation**, and the one that measured its endpoint twice by two rules graded itself right — "a reproduction, not a discovery". | half: the library's `docs/guide/bring-your-own.md` publishes the same id, the reference default and the full G6 tail (`:148`, `:348`) — **not fixed**; the cure there is a different endpoint, which is the operator's and round six's (library wishlist row 105) | **FIXED at this commit**: the sentence keeps its warning and drops the number — the `[warn]` row prints the id from *your* endpoint, and this page no longer prints it from the operator's |
| F-103 | cosmetic (a reader looked in the wrong directory first) [pass: claude-A (a2) finding 10 — "I looked in the wrong directory first. Name the path."; 2026-09-08, library CP-99] | "`pins.skeleton.json` written beside `rollout.yaml`" named none of the **three** `rollout.yaml` this estate writes (`work/runs/demo/`, `work/estate/`, `work/estate/receiver/`), and the one a reader has in their hands — the one the submit one-liner names and `preflight.py` reads — is not the one the skeleton is beside. | no | **FIXED at this commit**: the path is written out (`work/runs/demo/pins.skeleton.json`), the three are named, and the clause now says the thing a1 asked about — that on this door the walk the skeleton starts is already done by `bootstrap.py`'s own derivation (the library says the same inside the file since CP-99) |
| F-104 | costly (one wrong diagnosis, on the first command a reader runs) [pass: claude-A (a2) and claude-B (b1), independently; 2026-09-08, library CP-99] | the Install bullet carried pip's 15 s read timeout and its cure, but only one of its two signatures. When the timeout expires during the **resolver's** metadata fetch rather than a wheel download, pip does not say "timeout" at all: it prints `Could not find a version that satisfies the requirement pydantic … (from versions: none)` — a network fault that names a dependency and reads like a broken package. | half: the library's `docs/guide/trainer-guide.md` Install had **neither** signature — fixed there at library CP-99 | **FIXED at this commit**: both signatures under the same cure |
| F-105 | cosmetic → misleading (the warning was right about the mechanism and wrong about the size, in the direction that invites disbelief) [pass: gpt6-A (a1) read it four times on `vfs` and never identified its driver, filing nothing and paying 31 G; claude-B (b1) read it once, ran `docker info` before any project tool, and raised its own timeouts because of it; 2026-09-08, library CP-99] | the storage-driver warning priced `vfs` at "~13 GB per container and ~60 GB of image growth" and "a create takes minutes" — inferred once from round four's loaded host, never measured. Round five measured it against an `overlay2` control on the same door, corpus and row: **19.4 s vs 1.1 s** of sandbox init on the identical task, **31 G vs 8.4 G** of data root for the same 4.061 GB of images (3.7×, and 7.6× the images' own size; the other door's `vfs` container reproduced the class at 7.5×, 27 G for its own 3.588 GB). Mechanism confirmed, magnitude not — and round four's catastrophe did not recur. | half: the library's `estate.py` warning said the same and is re-worded there at library CP-99 (wishlist row 104) | **FIXED at this commit**: the numbers are the controlled pair's — in `bootstrap.py`'s warning and in the README's two `vfs` paragraphs (the disk bullet and the nested-daemon cure), which carried the same "~13 GB per sandbox container" — the loaded-host case keeps round four's 600 s budget and its ~60 GB of root growth as what they were, and the warning stays loud |

| # | severity | finding | library-side? | status |
| --- | --- | --- | --- | --- |
| F-91 | costly (one stranger ran a different episode from the one the README explains; one finished the walkthrough with the second retrieval tool never invoked) [pass: claude-A findings 1/2/5/6/7/8/11 and its unnumbered notes; codex-A findings 2/3 — 2026-09-07 (UTC), library CP-94] | The README's six, and what it under-sold: **(a)** the `up` printout's one-liner said `--row 0`, the walkthrough said `--row 2` — copy-pasting the printout runs a different episode; **(b)** the walkthrough never exercised the decisions surface (row 2 on a 27B: `decision_census.searched: false`, twice) although the section's own claim is that a real search "is the only way to see that the tool works"; **(c)** "The estate's agent has two retrieval tools" — the roster is eleven, G3 gates it, and the first call a stranger's episode made was `mcp_gsj_case_status`, never named; **(d)** `config.yaml.example` said the end-of-turn row "checks whichever is in effect" — after `up` it reads `work/estate/rollout.yaml`, measured with a deliberately wrong `end_of_turn_token_id: 151645` appended to `config.yaml` that changed nothing; **(e)** `work/sessions/` documented as "per-episode session dirs + agent logs" and empty after an accepted episode (Polar removes the gateway's per-session scratch after harvest; the harness's artifacts land in `work/estate/artifacts/<session_id>/`); **(f)** "and skips saying so" — a missing comma promised a silent skip. Under-sold: `page_census.pages_beyond_timestep` is the cutoff as a machine-assertable field and the README pointed only at `show`'s rendered string. Also: the export's version discriminator is the root key `format` (a stranger guessed `schema`); "arrays referenced by sha256" promised more than the `archive.sha256` + prose `where` that is there; the Run-it block put the venv before the clone for a reader who had already cloned to read it | no | FIXED at library CP-94: the printout keeps `--row 0` and says under it which row the README explains and how many rows the bank has; walkthrough step 3 submits a precedent row and reads `decision_census`; the Decisions section names the eleven-tool roster, G3, and the three MCP tools; `config.yaml.example` and the preflight paragraph say which file the row reads before and after `up`; the printout's `sessions/` line says what it is and where the artifacts are; the comma; the export paragraph names `format`, `page_census.pages_beyond_timestep`, `gate_findings` and the archive sha256; the Run-it block has the already-cloned line |
| F-90 | costly (the one un-probeable property is the one a borrowed endpoint's user cannot fix, and nothing downstream said so) [pass: claude-A finding 3 + codex-A's open question, 2026-09-07 (UTC), library CP-94] | Platform fact 2 ("the serve argv is yours to write"), `config.yaml.example`, the `up` printout and `preflight.py`'s `sampling defaults` row ("pin them server-side") all assume you serve the model. Both demo strangers were handed a URL they may not restart or re-argv — the normal case — so the advice was unusable, the episodes were accepted anyway, and nothing recorded that the sampling policy was unknown ("2,233 response tokens with logprobs and I do not know the temperature that produced them — the single largest unknown in the artifact I produced") | partly — the library's register row 22 (per-episode engine binding) is the pre-existing home; the library page has the same section since CP-94 | FIXED at library CP-94 for the README (Platform fact 2 names the two cases; "The borrowed endpoint" says what such traces are good for — provenance work, not training-distribution work) and the pins file (`provenance.engine.sampling_policy` and `coverage.sampling_policy` read `UNKNOWN` — what a trainer setting `GSJ_PINS_PATH` sees). `preflight.py`'s row text still says "pin them server-side" (not lifted): `waiting-on: a gsj-rollout-demo commit that opens preflight.py — the row gains the second case then` **[library CP-99] Scanned, not fired.** CP-99's freeze-lift NAMES `preflight.py` and this commit does not open it: round five filed nothing against the file (its `[warn]` row prints the id measured from the reader's own endpoint, which is why F-102's cure could be to drop the README's number), and the checkpoint's wall is "these findings only". The token stands, unchanged, on a commit that actually edits the file. |
| F-89 | costly (one stranger was "one keystroke from killing" a working pull; 21 minutes without a line against "~4 min") [pass: claude-A finding 4 (and the library door's b1/b2 independently), 2026-09-07 (UTC), library CP-94] | `docker pull` prints a line only when a layer changes state; the retrieval image's largest layer on a ~155 KB/s pipe printed nothing for 21 minutes (the image took 45 min; round two's took 48). The README named two pull *failure* signatures and no healthy-silent case; slow-versus-hung had to be established from `ps`, `/proc/net/dev` sampled twice and `docker system df` — "not a thing a reader will do" | no (`ensure_image` is the demo's own pull; the library's `image_pull` had the same gap and the same fix) | FIXED at library CP-94: `ensure_image` prints a heartbeat once a minute (elapsed, and whether this host received bytes in the last minute — a liveness signal, not the pull's own count; `GSJ_DEMO_PULL_HEARTBEAT_S` shortens it); the README's "The pull that is healthy and silent" gives the three checks before concluding a pull hung, and "~4 min" is now said to be a fast pipe's number |
| F-88 | costly (a `status` that suggested a second `up` during the first) [pass: codex-A finding 1, 2026-09-07 (UTC), library CP-94 — the library door's b1 hit the same shape on `estate status`] | `./bootstrap.py status`, run ≈19 minutes into an `up` that was still in its first image pull, printed `(gsj-demo: nothing running — ./bootstrap.py up)`, `(gsj-demo-polar: nothing running — ./bootstrap.py up)` and `(no bring-up record … — ./bootstrap.py up)`, exit 0 — literally true (the pulls precede the library tool, so no record, container or run lock could exist yet) and indistinguishable from "no setup ever started"; the stranger did not launch a second `up`, and said the README should say `status` cannot show an in-progress pull | no | FIXED at library CP-94: `up` and `down` hold `work/.bootstrap.lock` for their whole command (kernel flock, dropped with the process); `status` reads it and reports **ACTIVE** — who holds it, since when, wait, no suggestion; a second `up` is refused as busy; the README's status sentence names the three states |
| F-87 | costly → wrong shape ("accepted" on a foreign endpoint is a materially weaker statement than on the reference stack, and nothing said so) [pass: claude-A findings 10 + 11, 2026-09-07 (UTC), library CP-94] | On a `qwen3.6-27b` estate `work/estate/pins.gsj.json` had two of eight slots EMPTY (G4's tokenizer and chat-template hashes — correctly, no API exposes them), `tool_roster_hash` and `settings_hash` CARRIED from the reference captures, and `provenance.engine` describing another machine entirely — `Qwen/Qwen3-0.6B` at `127.0.0.1:8000` with HF snapshot paths that do not exist on that host (the packaged block, copied verbatim by `derive_pins`, which rewrote every provenance block but that one). The episode was accepted: `gate_findings: []`, `1 accepted, 0 quarantined`, and "nothing in `submit`, `read.py`, the export or the archive says so" — an accepted archive records no per-gate result, so "seven gates passed" and "five passed, two had nothing to check" read identically | partly — where the warning belongs is argued in the library's register row 87 (the pins file and the estate side; not `gsj_rollout/`) | FIXED at library CP-94 for the file and the README: `provenance.engine` is re-recorded for the endpoint `config.yaml` names (sampling policy and weights revision `UNKNOWN`, the G4 bytes `NOT MEASURED`) with the reference block kept under `carried_from`, labelled; a `coverage` block says per set derived-here / carried-and-checked / EMPTY; `up`'s pins line prints the same; the README's "What an acceptance covers" has the per-gate table. The reader's half — `./read.py` printing the coverage beside `accepted` (≈30 lines in `ls`/`show`/`export`, reading the pins file `GSJ_PINS_PATH` names) — is `read.py`, not lifted: `waiting-on: a gsj-rollout-demo commit that bumps read.py's export format (gsj-demo-episode-export/3) — a pin_coverage object rides it and show's header gains a pins line` **[library CP-96]** Rediscovered independently from the other door: a round-four B-door stranger (b2, the library from PyPI at 0.1.11, having read nothing of round three) wrote of `submit`'s `collected 1/1` and the accepted archive, "a rejection names its gate; an acceptance names nothing" — the second independent arrival, hours after `up`'s CP-94 pins line had printed the empty sets (the right stance at the wrong moment, F-63's shape again). The bump does not belong at CP-96 (`read.py` is not lifted and no wheel is cut) but its event is now named: the next demo sitting that opens `read.py`, which two independent rediscoveries mandate — the parked token above stands on it. |
| F-86 | costly (one stranger lost 52 min, another followed the wrong cause) [pass: claude-A finding 1 + gpt6-B finding 1, 2026-09-06; the second-round demo stranger's finding 1, 2026-09-06 (UTC) — library CP-92] | `bootstrap.py`'s pull-failure text (`ensure_image`, `:311-315`) names only the download-side causes — "if this host cannot reach ghcr.io, load the image out-of-band (docker save/load or skopeo) … a `no matching manifest` error would mean the registry lost this image's variant" — for ANY non-zero `docker pull`, and prescribes an out-of-band load. Two strangers' daemons downloaded every layer and failed to **extract** one (`failed to convert whiteout file … operation not permitted`; later no container could mount at all, `overlay … invalid argument` — a nested daemon with `/var/lib/docker` on overlayfs, the harness's fault, not the demo's), where a save/load fails on the very same layers; the third's retrieval-image pull retried two layers for 48 min and died `unexpected EOF` — a transport failure the conditional cure ("if this host cannot reach ghcr.io") did not describe, since the same registry had just served the Polar image, and no README recipe covered the retrieval image (only the historical Forgejo mirror). The third time this one `die()` line has misdiagnosed a non-network pull failure (F-54 → row 34 → this), each earlier fix appending a clause rather than telling download apart from extract | no — the demo's `bootstrap.py` (`ensure_image`, `check_docker`) | **CLOSED at library CP-93**, after PyPI 0.1.10 publication. `ensure_image` keeps pull progress on stdout, captures and re-emits stderr, and selects the existing download advice or a storage cure naming `docker run --rm alpine true`, `-v /var/lib/docker`, `--storage-driver vfs` and why save/load fails on the same layers. The local classifier matches the installed CP-92 rules; keeping it local lets `check_docker()` and a no-record `down` run before importing the library. `check_docker()` now runs `docker run --rm alpine true` after daemon/Compose checks and refuses if it fails (F-84 (d)); F-85 (c)'s help sentence moves with the floor. Measured: fourteen synthetic pull errors, including both captured stranger strings and transport/errno precedence, agree with the installed library classifier; three synthetic smoke paths pass; a real pull from unreachable `127.0.0.1:1` exits 1 with the unchanged download advice. Existing demo suite: 28 passed. The README's CP-92 two-outcome recipe remains. `waiting-on: nothing — closed` |
| F-85 | cosmetic [pass: the 2026-09-06 container round, library CP-92 — claude-A step-record line 83 and findings 6/11/12] | Three printed texts that disagree with the README or name no owner: **(a)** `synthetic/make_corpus.py`'s closing `next:` hint said "run ./bootstrap.py up", skipping the `validate` the README runs first (harmless — `cmd_up` validates before anything runs, README:272 says so — but a script-vs-README inconsistency); **(b)** the compose project names `gsj-demo` / `gsj-demo-polar` and the five container names appeared only in runtime output (`status`, the Polar-leg failure text, `read.py` hints), never in the README, which mentions no `docker logs` at all; **(c)** README:494-495 and `bootstrap.py`'s `--help` epilog say the sibling drop "remains the fallback through `--decisions-dir`" — a flag `bootstrap.py up --help` does not list, because it is the library estate tool's and `bootstrap.py` answers it through `work/bringup-answers.yaml` (README:276 and :569 name the owner; the Decisions sentence did not) | no | **FIXED at this commit** (library CP-92) for the three README/script sites: the hint reads `then ./bootstrap.py validate — or straight to ./bootstrap.py up, which validates first`; the five-containers paragraph names both compose projects, their containers, `docker logs <name>` and `status`; the Decisions sentence says the flag is the library tool's, answered by `bootstrap.py up` via the answers file, and that `bootstrap.py` has no such flag. **[library CP-93] (c) CLOSED in the docstring too**, after PyPI 0.1.10 publication: `--help` now names the library estate tool as the flag's owner, `work/bringup-answers.yaml` as the route, and says `bootstrap.py` has no such flag. Both help surfaces were executed; `up --help` correctly still lists no `--decisions-dir` |
| F-84 | cosmetic → costly (one stranger stopped, one guessed) [pass: three README-only strangers, 2026-09-06 (UTC), library CP-92 — gpt6-A findings 1/3, claude-A findings 2/3/4/5/9/10, the second-round demo stranger's findings 2/3] | The "Run it" block and the "What to expect" figures as three strangers read them: **(a)** the block created the venv before the clone with no word on where it lands (claude-A read it as a slip; gpt6-A, clone-first, finished with `.venv/` untracked in the repo — the demo's `.gitignore` does not list it); **(b)** two install figures sevenfold apart, 5.5 s (:53, "fast pipe") and 40 s (:61), condition unstated — a cold 36.6 s and a 63.3 s read as misses against the headline; **(c)** "Docker (compose v2)" read as a version pin now Compose ships 5.x, no definition or range anywhere; **(d)** the prerequisite was satisfied by a daemon that could not start any container — `bootstrap.py check_docker()` and the README both stop at `docker info` + `docker compose version`, and a nested daemon on overlayfs passed both and died 67 s into `up`; claude-A established the detector by measurement: `docker pull debian:stable-slim` succeeds there, only a `docker run` fails; **(e)** the closing section's `python -m pytest -q tests` needs pytest, which the install line does not bring; **(f)** `inference.model` had to be discovered by guessing `GET <base_url>/v1/models` — the README never says so (only `config.yaml.example:28-29` does; gpt6-A refused to open the template and stopped there; the second-round demo stranger guessed the route — the library stranger, on the estate guide, found it documented), and nothing said `inference.base_url` carries no `/v1` (one stranger's `ENGINE + /v1` was rescued by `validate`'s refusal); **(g)** preflight is placed only after `up`, though it runs as soon as `config.yaml` is filled — two strangers hesitated independently | no — README text; the prerequisite smoke belongs in `bootstrap.py check_docker()` too (not lifted at CP-92; recorded under F-86's event) | **FIXED at this commit** (library CP-92), each in the README: `mkdir -p gsj-demo && cd gsj-demo` names the directory the venv lands in and says the clone sits beside it (venv-first kept on purpose — the `.gitignore` is unchanged); ONE install figure with its condition (≈ 40 s cold from PyPI, CP-81; 36.6 s and 63.3 s measured by strangers on slow pipes; 5.5 s only with a warm pip cache, CP-61) and :61's parenthetical points back at it; "Docker with the `docker compose` plugin (Compose V2 or later — the Go plugin, not the legacy `docker-compose` v1; measured on 2.37.1 and 2.40.2)" plus the run smoke `docker run --rm alpine true` with the nested-daemon story and "the pull is not the check, the run is"; `pytest` named beside the pip line and at the suite sentence; the `/v1/models` recipe and the no-`/v1` rule as comments on the `cp config.yaml.example` line; a step-0 sentence saying preflight runs before `up` — every row but `tokenizer tail` answers, and on a non-reference model the `end-of-turn id` row settles only after `up` derives the id |
| F-83 | wrong shape [pass: the 2026-09-06 container stranger round, library CP-92 — claude-A finding 8] | `./read.py quarantine` on an archive with **zero** episodes — a fresh clone before its first submit, a `down --wipe`d estate, or a mistyped `--dir` — printed the green `the quarantine is empty (…) — every archived episode passed validation.` and exited 0, while its three siblings (`ls`, `show`, `export`) correctly refused `nothing archived under … — submit one first` with rc 1. Cause: `cmd_quarantine` scanned only `<dir>/quarantine` and never consulted `all_entries()` as its siblings do, and `scan()` returns `[]` for a missing directory. A stranger who ran every reader verb against an estate the harness never stood up read "every episode passed" over nothing at all; no test pinned the wording | no — the demo's own reader | **FIXED at this commit** (library CP-92): with no archived episode anywhere the verb dies with the siblings' text (rc 1, a nonexistent `--dir` included); the green sentence is earned only beside accepted bodies and now counts them the way `ls` does — by each body's findings, not by which directory it sits in — so a rejected wrapper at the top level (a `--dir` pointed at a `quarantine/` itself) refuses naming the traces root instead of being called accepted. Measured over a scratch directory holding copies of the four real capture bodies from `tests/fixtures` (`sk-polar-*.thinking-off.json`, no manifest, no synthetic): `4 accepted, 0 quarantined — the quarantine is empty (<dir>/quarantine): every archived episode passed validation.` rc 0; empty dir rc 1; missing dir rc 1; a top-level rejected wrapper `0 accepted; 1 rejected body sits in <dir> itself — point --dir at the traces root` rc 1; a populated `quarantine/` still lists. (`tests/fixtures` itself is not an archive — its `manifest.json` refuses under `quarantine` exactly as under `ls`.) Not test-pinned: `tests/` was outside CP-92's demo lift; the three measured invocations are the regression until the reader suite's next lift adds them |
| F-82 | wrong shape (evidence and input recovery; CP-82 audit B13–B15) | Synthetic mutations of real CP-81 traces showed positional result joins misclassifying a valid decision citation when tool results were reversed, and an EACCES write exported/displayed as written. A JSON array archive and scalar YAML config raised tracebacks. These are executed legal-shape/error fixtures, not claims of naturally reordered captures | no — demo reader and bootstrap input boundary | **[CP-86 phase 4] B13–B15 FIXED with regression tests; historical-capture proof PARTIAL.** One ID join drives show/export/census, missing/unmatched/ambiguous results are explicit, writes retain attempts and report succeeded/failed/unknown from results, malformed roots refuse with file/shape/cure. Export v2 documents changed fields. Three original CP-81 stranger bodies and CP-82's fresh body are byte-frozen and replayed; b4422728 remains ungrounded, the other three remain silent with hits available. CP-81 rehearsal bodies dd62726f… and c2aeed8a… were not recovered locally: their historical silence is not claimed as a new reader execution. `waiting-on: recover both original CP-81 rehearsal bodies with provenance and pass corrected-reader fixtures for all five; phase 6 must supply an operator-approved citation-policy truth table before any reward claim` |

**[library CP-43 — 2026-08-24] The audit's demo-side remainder closed**
in this repo's front-door sitting (the library's `docs/reports/CP-43.md`
is the record): W6 — `bootstrap.py` now empties G4's two hashes for ANY
non-reference model, derivation success or failure, so MODEL-SURFACE's
emptied-G4 claim is true unconditionally; W7 — the README draws the
corpus *source* tree as it is (`timestep-<T>/pages/page_NNNN.md`) and
names `md/` as the generated repo-side layout; W9's demo half — the
per-episode band now matches its own CP-36 measurements (~20–40 s);
C9 — `config.yaml.example` no longer says the eot default "silently
stands" (the bootstrap is loud about it); C10 — MODEL-SURFACE's
chat-template row now NAMES the four template-derived rows and excludes
the tool-parser and context-window rows (naming, not class-counting —
the context-window row is [endpoint]-classed on vLLM too, so "the four
[endpoint] rows" would re-embed the off-by-one); C11 — the generated
`pins.gsj.json` now carries THIS estate's `host`/`walk_status` and G1/G2
provenance instead of the reference H200's, and its `derived_at` records
what the run derived, carried, emptied, or FAILED to derive (the
failure-path artifact no longer claims endpoint-derived G6); M2 — the phantom
`healthcheck.sh` cite replaced with a self-contained description; and
S4's four demo texts — `bootstrap.py`'s eot docstring (the render-side
closer, not the engine's stop id — CP-38's measurement), `read.py`'s G6
cure (pins derive automatically at `up`, not a manual re-pin),
`preflight.py`'s "(Qwen3)" tail label, MODEL-SURFACE's one-estate
opener. No run pass, no new row; **next fresh id stays F-70**.

**[library CP-61 — 2026-08-30] The demo on the wheel — F-70–F-76 minted
by the first run of this repo as a consumer of library 0.1.3** (the
library's `docs/reports/CP-61.md` is the record; its wishlist row 51
carries the library-side halves). `bootstrap.py` stopped standing the
estate up itself: it reads `config.yaml`, hands the three values to the
library's own bring-up (`python -m gsj_rollout.bringup`, shipped in the
0.1.3 wheel) and adds only what that tool deliberately stops short of —
this estate's pins and the Polar leg as containers. The two reference
copies under `estate/` are gone (the packaged capture is the one source),
`gsj-polar` is rebuilt at 0.1.3, and `gsj-mcp-service:0.4.0` pulls
natively on Apple Silicon. Rows F-70–F-76 below are that run's register.

**[library CP-81 — 2026-09-03] The demo gains a decisions drop — thirty
court decisions written to bear on its own two cases — and F-80 is what
writing them found.** The library's decisions surface (its
`docs/decisions-surface.md`, built at library CP-79) serves real
`rii-dok v1` XML as Randnummern; the demo now generates thirty such
decisions beside its corpus, carries the §9.5 citation clause in its
`AGENTS.md`, and renders decision hits and `dec:` citations properly in
`./read.py`. Five episodes were collected against them, all accepted:
with the clause in the pinned prompt and a natural precedent task the
0.6B reference model searched, named the decisions by bare doknr in prose
and wrote **no** `dec:` token in four of them; told the exact token form
in the task prompt it wrote one and **fabricated the Randnummer** on hits
that carried none, which `read.py` reported as ungrounded at the source
(the README's "Decisions" section has the table and the transcript).
While the decisions were being written and
reviewed against `case_orchard`, a reviewer reading the case file as a
lawyer would found that the case file's own geometry contradicted itself —
F-80, fixed at the same commit.

| # | severity | finding | library-side? | status |
| --- | --- | --- | --- | --- |
| F-54 | **blocking (ARM hosts)** [pass 1, honest path] | `gsj-mcp-service:0.3.0` and `gsj-pi-harness:pi0.83.0-3` publish `linux/amd64` only (`gsj-polar` is multi-arch — the estate is asymmetric); an ARM docker REFUSES a manifest list with no matching platform rather than emulating, so `up` died at the mcp pull and, cured, again at the sandbox pull. The sandbox message misdiagnosed ("cannot reach ghcr.io"). What a stranger does: a docker-literate one reconstructs `docker pull --platform linux/amd64`; anyone else stops dead on the demo's headline promise | **yes** — the durable cure is an operator/publishing op: arm64 variants + a multi-arch index under the SAME pinned tags (provenance-sensitive; library wishlist row 40) | demo half FIXED at `96f2329`: bootstrap `ensure_amd64_image` (on ARM, absent → pull amd64 explicitly and say so; measured working — the mcp embed ran 115 s under emulation, episodes unaffected) + README ARM paragraph + truthful messages. Durable half: **mcp CLOSED at library CP-61** (`gsj-mcp-service:0.4.0` is a linux/amd64 + linux/arm64 index; the bootstrap pulls it natively), **sandbox CLOSED at library CP-64** (wishlist 40: `gsj-pi-harness:pi0.83.0-3` republished as a linux/amd64 + linux/arm64 index under the same pinned tag, the amd64 child byte-unchanged, pins-walk-verified) — the bootstrap's `--platform linux/amd64` cure, dead code from that day, retired at library CP-66 (a plain pull measured native on this arm64 daemon); every image pulls natively |
| F-55 | costly [pass 1] | README disk claim ~3.5 GB vs 6.3–7.1 GB measured | no | FIXED at `96f2329`: measured numbers, the old number named as compressed |
| F-56 | cosmetic [pass 1] | A transient GHCR TLS timeout fails `up` one-shot; the message is actionable and the re-run recovered | no | RECORDED, deliberately unfixed — a retry would mask real registry errors |
| F-57 | costly [pass 1, desk] | "fill in the three values" names two; the third (`corpus:`) lived only in the example file | no | FIXED at `96f2329` |
| F-58 | costly [pass 1, desk] | The host-local-endpoint rewrite (`host.docker.internal`) was absent from the README while its "no host ports" framing pointed the reader at the wrong worry | no | FIXED at `96f2329` |
| F-59 | cosmetic [pass 1, desk] | "Everything … lives under `work/`" vs the taskbank + lock written beside the corpus | no | FIXED at `96f2329` (the exception stated with its reason) |
| F-60 | costly [pass 1, desk] | The reference serve argv was nowhere; the desk-reader budgeted "30–60 minutes of vLLM wrangling" and predicted an issue asking for it | no | FIXED at `96f2329`: the argv block, with the two load-bearing flags (symmetric template, pinned generation config) called out as such |
| F-61 | cosmetic [pass 1, desk] | estate / Polar / pins / qualified undefined at first use; "five services" ambiguous against five containers | no | FIXED at `96f2329` (glossary block; the fix's own first draft re-embedded the ambiguity and was repaired pre-commit to "one published image, run here as three of those containers") |
| F-62 | cosmetic [pass 1, desk] | No row count for "row 0"; thinking levels unlisted | no | FIXED at `96f2329` |
| F-63 | costly [pass 3, both transcript readers] | `status COMPLETED` + `accepted` misread as task success; the outcome lived in a footer parenthetical ("correct design stance, wrong font size") | no | FIXED at `96f2329`: `show` header `deliverable` line ("acceptance checks provenance, not task success"); export `deliverable` object |
| F-64 | costly [pass 3, export reader] | `arrays.where` was an absolute path from this host in ad-hoc notation, with no integrity check | no | FIXED at `96f2329`: `archive.file`, `archive.sha256` (verified matching the body), `where` keyed to `archive.file`; made disposition-aware for quarantined bodies pre-commit |
| F-65 | costly [pass 3, export reader] | Span half-openness, mask polarity, and logprob alignment were recoverable only by arithmetic | no | FIXED at `96f2329`: the `conventions` block (each claim verified against a real body before being written down) |
| F-66 | cosmetic [pass 2/3] | The deliverable footer's "The skill asks for out/&lt;task_id&gt;.md" rendered on free-form rows that asked no such thing (`case_orchard@t2`) | no | FIXED at `96f2329`: conditional on `prompt_source`, with a third branch for bodies that name none |
| F-67 | wrong shape [pass 5] | The injected "missing prompts.yaml" is not an invalid corpus: contract §4 blesses it (`PASS 4 pages, 0 prompts`; the timestep contributes no tasks — the honest 0-count in the row is the guard) | no | NO FIX — not a defect; recorded so it is not re-filed as a bug |
| F-68 | cosmetic [pass 5] | read.py messages cited a README section by a nonexistent name | no | FIXED at `96f2329` |
| F-69 | costly, latent [library CP-40, audit O4 — minted by the three-repo audit, not by a run pass] | The estate depends on anonymous Forgejo read, and no document said so: compose sets `DISABLE_REGISTRATION` but no sign-in requirement, and the bootstrap's per-episode clone URL (`bootstrap.py` `clone_url_for`) embeds no credentials — anonymous read is load-bearing. Nothing is published to host ports, but the sandboxed agent lives on the same compose network by construction (it clones from it): a URL-guessing agent can re-clone `http://forgejo:3000/gsj-staging/<case_id>.git` and read past its timestep cutoff over the network. The in-sandbox git channel is closed (library CP-11: `--depth 1`, no remote, no reflog); this is the network channel — library gap row 2's estate residual, inherited unchanged | **yes** — the cure is estate posture (credentialed clone URLs or sandbox egress policy — library gap row 2); flipping sign-in on alone would break the credential-less clone flow, so the fix is a design, not a flag | **FIXED at CP-56 (this commit), in lockstep with the library.** `bootstrap.py` now mints a read-scoped token (`ensure_read_token`), embeds it in `clone_url_for` (the demo's library runs from the pinned 0.1.2 image, which predates `clone_credential_env`, so the token rides in the URL; `pi_harness` strips it from the trace echo), sets the generated mcp-config's `auth_token_env` to it, and — as the LAST bring-up step, after the frozen pipeline scaffolds/verifies and the MCP indexes, all anonymous — flips `REQUIRE_SIGNIN_VIEW` on via the env-gated compose (`GSJ_FORGEJO_SIGNIN`). The two re-clone channels then return 401 (proven container-faithful, library CP-56), the legitimate credentialed clone is unbroken, and the token stays out of traces. The two-phase is forced by the SAME frozen reads the library names — the demo works around them by flipping last, which the H200 (no bootstrap) documents as a manual two-phase. Master record: library charter §7 row 2's [CP-56] note. **[library CP-61]** superseded in mechanism, same posture: the library's bring-up mints the read token, creates Forgejo closed from its first start (no OFF→ON flip), names the token in `rollout.yaml` as `clone_credential_env` and keeps its value in `work/runs/demo/.env`; `submit` presents it by name |
| F-70 | wrong shape → fixed [library CP-61, the consolidation] | `bootstrap.py` and the library's `estate/bringup.py` were one design twice (library wishlist 48): the demo re-implemented Forgejo, tokens, scaffold, MCP, ingest, taskbank, verify and the run record for itself, and carried its own copies of the G2 capture and the reference AGENTS.md. Two things a stranger inherits from the change, said plainly: Forgejo and the MCP are now published on `127.0.0.1` host ports of the bring-up's choosing (its recipe — the old "no host ports" claim holds for the Polar leg only), and the `validate` table prints twice at `up` (once by this script, before any image is pulled, once by the bring-up itself) | **yes, DONE** — the bring-up ships in 0.1.3 (library wishlist 48 CLOSED) | FIXED at this commit: `bootstrap.py` 1,241 → 1,058 lines — a reader: `config.yaml` → `work/bringup-answers.yaml` → `python -m gsj_rollout.bringup up --answers … -y` from `work/` (the run lands at `work/runs/demo/`), then the demo's pins and its Polar containers; `estate/system_prompt.reference.txt` (sha `f56e8a6e…`, byte-equal to the packaged capture) and `estate/AGENTS.reference.md` (sha `5308b28e…`, byte-equal to the span pi's `<project_instructions>` markers delimit in that capture) retired; `gsj-polar:f0e8343a-gsj0.1.3` rebuilt multi-arch; `clone_credential_env` used as designed (the token named in `rollout.yaml`, its value only in `work/runs/demo/.env`, absent from both archived traces). Measured on the working tree with images present: `up` 51.2 s, warm 13.3 s, after `down` 33.7 s, one accepted episode 21 s |
| F-71 | costly [library-side, worked around] | The bring-up's emitted `rollout.yaml` addresses a Polar that runs on the HOST — `polar.rollout.host: 127.0.0.1` with no `public_url`, `receiver.host: 127.0.0.1`, `traces_dir`/`harness.artifacts_dir` as host paths — and no flag says "Polar runs elsewhere"; run as containers, that config's rollout server and receiver are unreachable and the receiver writes into a path that does not exist | **yes** — library wishlist 51 (a) | WORKED AROUND at this commit: `containerize_rollout_yaml` re-addresses exactly those values into `work/estate/rollout.yaml` (0.0.0.0 + the compose DNS `public_url`s, `/estate/traces`, `/estate/artifacts`) and keeps everything else the bring-up wrote; the bring-up's own file stays at `work/runs/demo/rollout.yaml` as written |
| F-72 | costly [library-side, worked around] | `--mcp create` checks that the image is PRESENT and never pulls, and its default (`gsj-mcp-service:0.4.0`) is a local build tag that exists on no registry — a wheel consumer who runs the tool bare is refused at the MCP step with a `docker build` recipe pointing at a checkout they do not have | **yes** — library wishlist 51 (b) | WORKED AROUND at this commit: `ensure_image` pulls `ghcr.io/mhganainy/gsj-mcp-service:0.4.0` (published multi-arch at library CP-61 — F-54's mcp half is cured natively) before the bring-up runs, and the answers file names it as `mcp_image` |
| F-73 | costly [library-side] | Every `up` prints `[bringup] pins — WARNING: 1/1 skill card(s) are not in the packaged approved set (G1): ['brief'] — episodes on them quarantine until the pins walk re-derives` — false for this estate: the bring-up's G1 check reads the library's PACKAGED set even though `GSJ_PINS_PATH` names the pins this script derived one step earlier (which approve exactly that card); the episode on the `brief` row was accepted at G1 | **yes** — library wishlist 51 (c) | **CLOSED at library CP-62**, shipped in 0.1.4: the check uses the same pins resolver as the library, including `GSJ_PINS_PATH`; CP-63's installed-wheel stranger run printed the derived set and no false warning. **[library CP-83, audit phase 1]** Rechecked on installed 0.1.7: this corpus's `brief` hash in a named scratch pins set gives `pins_source: GSJ_PINS_PATH`, `not_in_approved_set: []`. The stale disposition is corrected; library row 22's independent trace-to-engine provenance gap remains open. `waiting-on: nothing — closed` |
| F-74 | cosmetic [library-side] | On an Apple Silicon daemon the bring-up announces `defaulting to the native image gsj-mcp-service:0.4.0-arm64 (the amd64 one dies under qemu — wishlist 49)` BEFORE it reads the `mcp_image` answer; the published `ghcr.io/…:0.4.0` ran (run.json, the generated compose and the container agree) — the line said otherwise | **yes** — library wishlist 51 (d) | RECORDED; **[library CP-62] cured** (51 (d): the line prints after the answer is read, only when the native build is what runs, and only from the checkout — the wheel's default is the published index), rode 0.1.4; row 51 closed in full at library CP-65 |
| F-75 | costly [pass: the second episode] | A second `submit` while one episode is still running answers `gsj-rollout: rollout server unreachable or errored at http://polar-rollout:8081: Client error '409 Conflict'` — the client submits every task under the fixed id `gsj-task` and Polar refuses a duplicate; nothing in the message says `--task-id <another>` exists. Not new to this commit (the CP-36 demo had the same client), first measured now | **yes** — library wishlist 51 (f) | README: the walkthrough says so beside the one-liner (`--task-id` for a second concurrent episode; otherwise wait for the first) |
| F-76 | costly [pass: the first episode] | The floor model's skill-card episode (`case_mill@3`, row 0) never ended on its own: 463 assistant turns in 7 minutes of `read /workspace` → `EISDIR`, pi compacting its context when the window filled (usage reset to 0), the gateway proxying ~1 completion/s — until the operator stopped the sandbox (the receiver then quarantined it correctly: `ADM1:status_not_completed:ERROR`). The ceiling that WOULD have ended it is the library's per-session `timeout_seconds` (900 s) — which the README's "one episode: ~20–40 s" never mentioned | no — the ceiling exists; the expectation was unstated | README: the expectation now states the 15-minute ceiling and that a looping small model burns all of it; the demo's example row is 1 (`case_orchard@2`, 2 turns, 21 s) **[library CP-83, audit phase 1]** The CP-81 six-row bank moved that walkthrough to row 2 (`case_orchard@2`, `free:boundary-evidence`); row 1 is now `case_mill@3` / `skill:brief`. Both README commands now select row 2, proved by a fresh accepted/read episode with two turns and one case search; the historical looping skill-task warning remains valid. |
| F-77 | cosmetic [pass: the Polar leg's logs] | `docker logs gsj-demo-receiver` — where the bootstrap sends a stranger whose Polar leg did not come up — opens with the library's `run Polar's two processes yourself:` block (`PYTHONPATH=<checkout> <checkout>/vendor/polar/.venv/bin/polar serve_rollout …`): `gsj-rollout serve` prints the F-45 printout on every start, container or not. The library's wishlist row 37 closed this printout as cosmetic "until a register row from a container user who read it" — this is that row | **yes** — library wishlist 37 (reopened; **closed at library CP-65** under ADR-0028) | the compose file and the receiver's own directory say the block is not for the reader; the containers beside it ARE those two processes. **[library CP-65]** the printout recognizes the co-installed shape — a `polar` on PATH beside the interpreter prints a bare runnable pair, no `PYTHONPATH=<checkout>`, no NOTE; it rides the next release (0.1.4 predates it) |
| F-78 | **blocking (any host without the image)** [pass: the from-nothing run, library CP-61] | `codeberg.org/forgejo/forgejo:16.0.2` — the tag the library's bring-up pins (`FORGEJO_IMAGE` in `gsj_rollout/bringup.py`, frozen this CP) and this repo's old compose pinned — is unpullable: codeberg's registry still serves the tag's index, but BOTH platform manifests it lists answer 404 (`failed to copy: httpReadSeeker: failed open: content at …/manifests/sha256:c7fb3ea8… not found`; measured 2026-08-30 with an anonymous registry token: 16.0.1, 16.0.3, 16.0 and 16 answer 200 on both platforms, 16.0.2 answers 404 on both). Every earlier run had the image locally (pulled 2026-08-18); the from-nothing run died at the bring-up's `docker compose up forgejo` with its out-of-band-load refusal. The demo cannot route around it: the bring-up has no `--forgejo-image`, and re-tagging a live 16.0.x as 16.0.2 would lie about provenance | **yes** — library wishlist 52 (a re-pin to a live tag, contract-checked — the bring-up's admin CLI and token-minting measurements were made on 16.0.2 — rides `bringup.py` in the next wheel) | RECORDED; the CP-61 from-nothing run executed the bring-up's own cure — `docker save` on a host that holds the image (the H200) piped into `docker load` here (amd64, emulated on this Apple Silicon daemon) — and the README's troubleshooting names it. **[library CP-62] CLOSED library-side, and a cure with no second host found.** Re-measured 2026-08-30 16:51 UTC: 16.0.2 still dead on codeberg (both platforms), 16.0.1/16.0.3 alive — and the Forgejo project's mirror `code.forgejo.org/forgejo/forgejo:16.0.2` serves the SAME image (index `sha256:2fdfe28b…`, both platform manifests 200), so `docker pull code.forgejo.org/forgejo/forgejo:16.0.2 && docker tag … codeberg.org/forgejo/forgejo:16.0.2` gives library 0.1.3's bring-up the very bytes it pins, no provenance lie, no second host (the README's "Not normal" recipe; measured on the from-nothing leg below). The library re-pinned to `16.0.3` (measured pullable) and added `--forgejo-image <ref>` (recorded in `run.json`; named in the pull refusal), which `./bootstrap.py up --forgejo-image` forwards; both rode out on 0.1.4 (PyPI, library CP-63 — this bootstrap floors at >=0.1.4), so a PyPI stranger no longer needs the mirror recipe; it stays in the README as the historical cure for anyone pinned to 0.1.3 |
| F-80 | wrong shape [pass: writing decisions against the corpus, library CP-81] | **The synthetic corpus's own boundary geometry was inverted, and had been since the corpus was written.** `case_orchard` page 2 read "Ortiz's plan places the old wire fence 3.2 metres WEST of the registered boundary line … the strip between fence and registered line is therefore … part of parcel 114/3 — the respondent's parcel". The parcels are 114/2 (claimant) west and 114/3 (respondent) east, so a fence WEST of the registered line puts the land between them west of the line — inside 114/2, the CLAIMANT's parcel — contradicting page 2's own conclusion; and it puts the strip EAST of the fence, contradicting page 1 ("a strip … currently on the western side of an old wire fence, used by the claimant"). Only the compass word was wrong: with the fence 3.2 metres EAST of the registered line, the strip lies west of the fence (page 1) and east of the registered line, hence in 114/3 (page 2's conclusion), and page 3's ditch "on the western side" and page 4's easement against 114/2 both cohere. Nothing in the estate noticed, and nothing could: the pages are content, not schema — `validate` checks the tree, the pins hash `AGENTS.md` and the skill cards, and no gate reads a page's prose. It surfaced only when thirty decisions were written to bear on this case and a reviewer read the case file as a lawyer would | no — the corpus is this repo's own fixture | **FIXED at this commit** (library CP-81): page 2 reads EAST, and the sentence names the strip as lying between the registered line and the fence. The pages are regenerated by `./synthetic/make_corpus.py`, so a stranger gets the coherent version; an estate built before this commit keeps the old page bytes until its next `up` (the scaffold pushes what the tree holds). The lesson is the general one: a demo corpus is read by whoever is deciding whether to trust the system, so its content is part of the deliverable, not filler |
